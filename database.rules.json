{
  "rules": {
    // Permite leitura e escrita baseada no frontend, extendendo a data de validade para o ano 2100
    // Isso evita bloqueios de expiração padrão do Firebase de modo 'teste'
    ".read": "now < 4102444800000",
    ".write": "now < 4102444800000"
  }
}
